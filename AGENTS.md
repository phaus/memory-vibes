# Project Agents

This repository uses a simple set of **agents** (scripts) to streamline common development tasks. They are primarily wrappers around standard commands.

## Build Agent

```bash
# Configure and build the project (Release mode)
mkdir -p build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build .
```

The resulting executable is `./mem_band` (or `Release\mem_band.exe` on Windows).

## Run Agent

```bash
# Run the benchmark with default options
./mem_band

# Example with custom parameters
./mem_band --size 1024 --iters 30 --type double
```

## Test Agent

The project does not include a full test suite yet, but you can perform a quick sanity check:

```bash
./mem_band --size 64 --iters 2 --type float
```

If the command exits with code `0` and prints the expected CSV‑friendly output, the build is considered functional.

## Documentation Update Agent

When adding new features or changing the interface, update the `README.md` accordingly. The steps are:
1. Edit the relevant sections (Build Instructions, Usage, Testing).
2. Commit the changes with a concise message.
3. Push to the remote repository.

---
*Generated by the OpenCode assistant.*
